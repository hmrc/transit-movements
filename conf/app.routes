# microservice specific routes

POST    /departures                 controllers.DeparturesController.sendDeclarationData
POST    /departures/:id/messages    controllers.DeparturesController.receiveMessage(id: DepartureId)

POST    /arrivals                   controllers.ArrivalsController.sendArrivalNotification
POST    /arrivals/:id/messages      controllers.ArrivalsController.receiveMessage(id: ArrivalId)
